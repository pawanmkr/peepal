export enum UserRole {
    PROFESSIONAL = 'user',
    USER = 'user',
}

export enum ChargeType {
    HOURLY = 'hourly',
    PER_SESSION = 'per_session',
    PER_MONTH = 'per_month',
    PER_WEEK = 'per_week',
    PER_DAY = 'per_day',
}
