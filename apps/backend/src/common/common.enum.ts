export enum UserRole {
    PROFESSIONAL = 'professional',
    USER = 'user',
}

export enum ChargeType {
    HOURLY = 'hourly',
    PER_SESSION = 'per_session',
    PER_MONTH = 'per_month',
    PER_WEEK = 'per_week',
    PER_DAY = 'per_day',
}
